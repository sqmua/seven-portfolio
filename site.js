const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.08});
const featured=document.querySelector('#featured'),archive=document.querySelector('#archive'),link=p=>`project.html?id=${PROJECTS.indexOf(p)}`;
if(featured){const picks=PROJECTS.filter(p=>p.featured);featured.innerHTML=picks.map((p,i)=>`<article class="feature ${i===0?'hero-case':''}"><a href="${link(p)}"><div class="feature-img"><img src="${p.cover}" alt="${p.title}" ${i<2?'fetchpriority="high"':'loading="lazy"'}><span>查看案例 ↗</span></div><div class="feature-copy"><div class="number">${String(i+1).padStart(2,'0')}</div><div><p>${p.date} · ${p.type}</p><h3>${p.title}</h3><h4>${p.client}</h4><div class="feature-desc">${p.summary}</div></div></div></a></article>`).join('');const renderArchive=filter=>{const items=PROJECTS.filter(p=>!p.featured&&(filter==='all'||p.tags.includes(filter)));archive.innerHTML=items.map(p=>`<article><a href="${link(p)}"><div><img src="${p.cover}" alt="${p.title}" loading="lazy"><span>${p.year}</span></div><p>${p.type}</p><h3>${p.title}</h3><h4>${p.client}</h4></a></article>`).join('')||'<p class="empty">此分类的项目正在整理。</p>';document.querySelectorAll('.archive article').forEach(el=>observer.observe(el))};renderArchive('all');document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelector('.filters .active').classList.remove('active');btn.classList.add('active');renderArchive(btn.dataset.filter)}))}
document.querySelectorAll('.feature,.archive article,.cap-grid article,.timeline article').forEach(el=>observer.observe(el));
